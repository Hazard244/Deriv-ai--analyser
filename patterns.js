const Patterns=(()=>{function markov(n=250){return EvenOdd.markov(n)}function sequence(n=100){const a=EvenOdd.recent(n);if(a.length<2)return{alternation:0,repetition:0};let s=0;for(let i=1;i<a.length;i++)s+=a[i].type!==a[i-1].type;return{alternation:s/(a.length-1)*100,repetition:100-s/(a.length-1)*100}}function bias(){const a=EvenOdd.recent(25),b=EvenOdd.recent(250),r=(x,t)=>x.length?x.filter(q=>q.type===t).length/x.length*100:50,se=r(a,'EVEN'),le=r(b,'EVEN'),so=100-se,lo=100-le;return{direction:se-le>3?'EVEN':lo-so>3?'ODD':'NEUTRAL',evenShift:se-le,oddShift:so-lo}}function snapshot(){return{markov:markov(),sequence:sequence(),bias:bias(),streak:EvenOdd.streak()}}return{markov,sequence,bias,snapshot}})();
