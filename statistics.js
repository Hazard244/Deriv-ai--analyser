const Statistics=(()=>{const ticks=n=>EvenOdd.recent(n);function entropy(n=100){const a=ticks(n);if(!a.length)return 1;const e=a.filter(x=>x.type==='EVEN').length/a.length,o=1-e;return(e?-e*Math.log2(e):0)+(o?-o*Math.log2(o):0)}function imbalance(n=50){const a=ticks(n),e=a.filter(x=>x.type==='EVEN').length,o=a.length-e,v=a.length?(e-o)/a.length*100:0;return{percentage:v,direction:v>0?'EVEN':v<0?'ODD':'NEUTRAL'}}function switchRate(n=100){const a=ticks(n);if(a.length<2)return 0;let s=0;for(let i=1;i<a.length;i++)s+=a[i].type!==a[i-1].type;return s/(a.length-1)*100}function alignment(){const w=EvenOdd.snapshot().windows;let e=0,o=0,t=0;Object.values(w).forEach(x=>{if(x.samples<10)return;t++;x.evenProbability>x.oddProbability?e++:x.oddProbability>x.evenProbability&&o++});return{direction:e>o?'EVEN':o>e?'ODD':'NEUTRAL',alignment:t?Math.max(e,o)/t*100:0,evenVotes:e,oddVotes:o,total:t}}function snapshot(){return{windows:EvenOdd.snapshot().windows,entropy:entropy(),imbalance:imbalance(),switchRate:switchRate(),alignment:alignment()}}return{entropy,imbalance,switchRate,alignment,snapshot}})();
